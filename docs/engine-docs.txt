/** @page engine_docs Engine docs
This page documents details about the UCI engine executable.

@tableofcontents{html}

@section engine_commands Commands

@ENGINE_COMMANDS_HELP_TEXT@

@section engine_options Options

@ENGINE_OPTIONS_HELP_TEXT@

@section engine_cli CLI options

On startup, the engine will process any command-line arguments as if they were a single UCI command.

The following additional flags may also be specified:
@li ``--no-logo``: prevents the engine from printing the logo and version on startup.
@li ``--no-loop``: processes the initial UCI command line and then exits, without entering the UCI event loop. Useful for scripts.

These flags may be given before or after the other CLI arguments.

For example, the documentation for the engine commands is generated by a script that calls: @verbatim ben_bot --no-loop --no-logo help --no-logo @endverbatim The first ``--no-logo`` is the global CLI flag, and the second one is an argument to the ``help`` UCI command.
 */
